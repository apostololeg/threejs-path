#!/bin/sh

export $(grep -v '^#' .env | sed 's/ //g' | xargs) && \
    rsync -av -e ssh src/client/build root@${PROD_IP}:${PROD_PATH} --progress
    # rsync -av -e ssh --exclude='face_mesh/' --exclude='assets/'  build root@${PROD_IP}:${PROD_PATH} --progress
