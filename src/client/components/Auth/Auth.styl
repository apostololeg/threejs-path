.root
  position fixed
  top 0
  left 0
  right 0
  bottom 0
  display flex
  flex-direction column
  align-items center
  justify-content center
  height 100vh
  width 100vw
  backdrop-filter blur(50px)

.fields
  display flex
  flex-direction column
  width 200px
  max-width calc(100vw - 20px)
